{
  "name": "emotrust-hardhat",
  "private": true,
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run ./deploy.js --network sepolia"
  }
}

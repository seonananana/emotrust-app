# ========== OpenRouter ==========
# OpenRouter API 키(없으면 폴백으로 동작)
OPENROUTER_API_KEY=sk-or-v1-여기에_진짜_키_붙여넣기

# 사용할 모델(비용 아끼려면 7B)
LLM_MODEL=mistralai/mistral-7b-instruct
# LLM_MODEL=mistralai/mixtral-8x7b-instruct

# 응답 토큰 상한(비용 제어)
MAX_TOKENS=64
REQUEST_TIMEOUT_SECS=20

# (선택) OpenRouter 권장 헤더
# OPENROUTER_HTTP_REFERRER=https://your.app
# OPENROUTER_TITLE=emotrust-dev

# ========== Fallback Policy ==========
# 402(크레딧 부족) 시 폴백 사용
FALLBACK_ON_402=1
# 기타 오류/타임아웃 시 폴백 사용
FALLBACK_ON_ERROR=1

# ========== CORS ==========
# 개발: 전부 허용(*) / 배포: 오리진 나열(쉼표로 분리)
ALLOW_ORIGINS=*

# ========== ngrok ==========
# 고정 도메인이 있다면 우선 사용 (예: https://api.example.com)
# PUBLIC_BASE_URL=
# ngrok 로컬 API 주소(WSL에서 ngrok 같이 돌릴 때 그대로 OK)
NGROK_API_URL=http://127.0.0.1:4040

# ========== 로깅 ==========
LOG_LEVEL=INFO

# ========== csv ==========
EMO_LEXICON_PATH=backend/data/nrc_words.csv
EMO_LEX_WORD_COL=word
EMO_LEX_SCORE_COL=score

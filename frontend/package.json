{
  "name": "emotrust-app",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "prestart": "node ./update-env-from-ngrok.mjs",
    "start": "expo start -c",

    "preandroid": "node ./update-env-from-ngrok.mjs",
    "android": "expo start --android -c",

    "preios": "node ./update-env-from-ngrok.mjs",
    "ios": "expo start --ios -c",

    "preweb": "node ./update-env-from-ngrok.mjs",
    "web": "expo start --web -c"
  },
  "dependencies": {
    "expo": "~53.0.20",
    "expo-document-picker": "^13.1.6",
    "expo-status-bar": "~2.2.3",
    "react": "19.0.0",
    "react-native": "0.79.5"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0"
  }
}
